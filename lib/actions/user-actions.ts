'use server';

// Temporary empty file to prevent Turbopack errors from transitive dependency on firebase-admin
// export async function autoCreateUserAction(userData: any) {
//     return { success: false, error: "Disabled" };
// }
